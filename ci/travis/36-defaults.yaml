name: test
channels:
  - conda-forge
  - mullenkamp
  - defaults
dependencies:
  - python=3.6
  # required
  - geopandas
  - pycrs
  - pdsql
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
