name: test
channels:
  - conda-forge
  - mullenkamp
  - defaults
dependencies:
  - python=3.7
  # required
  - geopandas
  - pycrs
  - pdsql
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
    - overpass
    - osm2geojson
