name: test
channels:
  - conda-forge
  - mullenkamp
  - defaults
dependencies:
  - python=3.5
  # required
  - pandas
  - geopandas
  - shapely
  - pycrsx
  - rasterio
  - scipy
  # testing
  - pytest
  - pytest-cov
  - pip:
    - codecov
